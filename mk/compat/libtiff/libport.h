/*
 * libport.h -- stand-in for libtiff's port shim, which supplies getopt(),
 * strcasecmp() and friends on platforms that lack them.  Darwin has all of
 * them in libc, so this is deliberately empty.
 */
#ifndef _DARWINTOOLS_LIBPORT_H_
#define _DARWINTOOLS_LIBPORT_H_
#endif
